services:
  postgres:
    image: postgres:15
    container_name: syllab-postgres
    environment:
      POSTGRES_USER: syllab_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: syllab_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: syllab-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:

