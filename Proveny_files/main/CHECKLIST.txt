Proveny Final Deliverable — Self-Verification Checklist
======================================================

[ ] Application is LIVE on dashboard.deployrocks.com with working public URL
[ ] docker compose up boots entire stack (backend + frontend + postgres + redis) locally
[ ] pytest / jest runs and passes with full test suite
[ ] Swagger UI / API docs is accessible on the deployed URL and covers ALL endpoints
[ ] All auth endpoints work on deployed frontend: register, login, logout, refresh, RBAC protected routes
[ ] Email verification sends REAL email on signup using 3rd party service
[ ] Password reset sends REAL email with working reset link
[ ] At least 3 business event emails are implemented and queued via background worker
[ ] Frontend demo consumes real backend API and demonstrates all major workflows
[ ] All business logic endpoints from blueprint are implemented and testable
[ ] Background workers are running and processing jobs
[ ] No raw SQL queries exist in codebase (ORM only) — EXCEPTION: cohort window stats use one Prisma $queryRaw for PERCENT_RANK/STDDEV
[ ] Video link is accessible and 4-6 minutes long
[ ] Postman collection with ALL endpoints loaded in separate tabs ready for defense
[ ] I am the sole author of the architectural decisions in this code

Implemented business emails (queued via BullMQ email worker):
  1. Baseline capture confirmation (student)
  2. Submission flagged for review (instructor)
  3. Critical risk escalation alert (instructor)
  4. Interview outcome notification (student)
  5. Stale flagged-review reminder (instructor, maintenance cron)

Role-based frontend views after login:
  - STUDENT    → baseline upload, assignment submit, forensic history
  - INSTRUCTOR → courses, enrollments, assignments, action queue, one-shot analysis, cohort reports
  - PROCTOR    → proctored session create / activate / lock
  - ADMIN      → user provisioning, audit logs

Local quick test:
  docker compose up --build
  Frontend: http://localhost:3001
  API/Swagger: http://localhost:3000/docs (also proxied at http://localhost:3001/docs)
  npm test
